mutation upsertUser(
  $name: String
  $displayName: String
  $iconImageUrl: String
) {
  upsertUser(
    input: {
      name: $name
      displayName: $displayName
      iconImageUrl: $iconImageUrl
    }
  ) {
    ...UserPrivateField
  }
}
